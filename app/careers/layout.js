export const metadata = {
  title: "Careers | Coordly",
  description:
    "Join the mission to build the operating system for next-gen teams. Remote-first, open values.",
};

export default function CareersLayout({ children }) {
  return <>{children}</>;
}
