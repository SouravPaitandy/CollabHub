export const metadata = {
  title: "Pricing | Coordly",
  description:
    "Premium collaboration features, now completely free. No credit card required.",
};

export default function PricingLayout({ children }) {
  return <>{children}</>;
}
