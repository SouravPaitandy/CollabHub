export const metadata = {
  title: "Login / Register | Coordly",
  description: "Initialize your session and enter the unified workspace.",
};

export default function AuthLayout({ children }) {
  return <>{children}</>;
}
